<h1>物件一覧</h1>

<table>
  <% @rooms.each do |room| %>
  <tr>
    <th>物件名</th>
    <td><%= room.name %></td>
    <th>賃料</th>
    <td><%= room.rent %></td>
    <th>住所</th>
    <td><%= room.address %></td>
    <th>築年数</th>
    <td><%= room.age %></td>
    <th>備考</th>
    <td><%= room.note %></td>

  </tr>
  <%# <td><%= link_to '詳細', room_path %></td> %>
  <%# <td><%= link_to '編集', edit_room_path(room) %></td> %>
  <%# <td><%= link_to '削除', room, method: :delete, data: { confirm: 'Are you sure?' } %></td> %>
  <% end %>
  </tr>
  <% end %>
</table>

<br>

<%= link_to '登録', new_room_path %>